import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testetecnico.superfrete',
  appName: 'teste-tecnico-superfrete',
  webDir: 'build'
};

export default config;
